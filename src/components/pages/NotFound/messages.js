import { defineMessages } from 'react-intl'

const ns = 'pages.notFound'

export default defineMessages({
  notFound: {
    id: `${ns}.notFound`,
    defaultMessage: 'Not found',
    description: 'Not found page header'
  }
})
