import { defineMessages } from 'react-intl'

const ns = 'pages.home'

export default defineMessages({
  home: {
    id: `${ns}.home`,
    defaultMessage: 'Home',
    description: 'Homepage header'
  }
})
